# 2026-06-05 → 2026-06-12 — One-Week Summary

About one week of work spanning torchtitan development on Aurora/Sunspot and
the broader project. ~140 of-my-own commits on `ezpz` (the rest are upstream
merges and cherry-picks), 71 PBS jobs, 6 long-form comments on
[saforem2/torchtitan#14](https://github.com/saforem2/torchtitan/pull/14), six
upstream syncs (50th–55th).

## 1. Production training (active chains)

- **agpt-2B 256N canonical chain**: advanced to step 74,700 ≈ 3.76T tokens
  (80.5% of 4.67T target) — cont10 (`8521630`) transitioned Q→R 2026-06-12
  14:42 after **9 days of `small` queue contention**, producing +400 steps
  in its first hour. Trajectory:
  [`production/agpt/2b/n256/`](../production/agpt/2b/n256/README.md). Charts
  refreshed and re-rendered in the new ambivalent + Iosevka style
  ([`d3fbfbf18`](https://github.com/saforem2/torchtitan/commit/d3fbfbf18),
  [`1b43fb152`](https://github.com/saforem2/torchtitan/commit/1b43fb152)).
- **agpt-20B 512N**: advanced step-4400 → **step-4500** on 2026-06-10
  (`8521628`), persisted DCP cleanly, then crashed at the next
  `set_determinism` init step (`std::bad_alloc`, same failure mode as
  8466848). Cont (`8521632`) Q'd for next 512N slot.
  ([`production/agpt/20b/n512/`](../production/agpt/20b/n512/README.md)).
  Walltime-cutoff resumes for steps 2400/4200/4300 eval-only:
  [`d2fddb808`](https://github.com/saforem2/torchtitan/commit/d2fddb808),
  [`e1d98904a`](https://github.com/saforem2/torchtitan/commit/e1d98904a),
  [`87320ee73`](https://github.com/saforem2/torchtitan/commit/87320ee73),
  [`e48871e8c`](https://github.com/saforem2/torchtitan/commit/e48871e8c).
- **agpt-2B 256N v1 backfill**: 23 ckpts re-evaluated through the new 7-task set
  after `8533608` walltimed ([`e30c46ca6`](https://github.com/saforem2/torchtitan/commit/e30c46ca6)).
- **80B NaN diagnosis at n=32**: completed a 4-test factorial (fp32 master,
  TP=4 GBS96, TP=4 GBS192, seed shift —
  [`8979fb193`](https://github.com/saforem2/torchtitan/commit/8979fb193),
  [`fd27c9632`](https://github.com/saforem2/torchtitan/commit/fd27c9632),
  [`f806b3419`](https://github.com/saforem2/torchtitan/commit/f806b3419),
  [`3aa9bbd0a`](https://github.com/saforem2/torchtitan/commit/3aa9bbd0a)).
  Initial bf16-overflow hypothesis
  ([`fceb5a361`](https://github.com/saforem2/torchtitan/commit/fceb5a361))
  refuted ([`779baccca`](https://github.com/saforem2/torchtitan/commit/779baccca)).
  Tight-clip test (`max_norm=0.1` vs `1.0`,
  [`b260e7052`](https://github.com/saforem2/torchtitan/commit/b260e7052))
  also refuted ([`70df43a17`](https://github.com/saforem2/torchtitan/commit/70df43a17)).
- **80B NaN diagnosis revisions (2026-06-12)**, three in one day:
  - **Morning**: `--debug.seed=42 --debug.deterministic` at n=32 GBS=192
    (`8539896` + `8539982`) trained clean 20 steps — declared "XPU
    nondeterministic op is the root cause", recommended `--debug.deterministic`
    as the production fix.
  - **Afternoon**: scaled the determinism fix to n=64 GBS=384 (`8540102`)
    with overprovisioning (select=68, FAILOVER_MAX_RETRIES=2). Trained 8
    clean steps then `grad_norm=inf` step 3 (recovered via clip),
    `grad_norm=nan` step 9 → `loss=nan` step 10, NaN persisted through
    step 20. **Refutes the determinism hypothesis** — n=32 success was
    a lucky sample, not causal. Doc revised
    ([`434cfe5d1`](https://github.com/saforem2/torchtitan/commit/434cfe5d1))
    with new "🚨🚨 Counter-evidence" section.
  - **Evening**: queued `--training.mixed-precision-param=float32` at TP=4
    n=64 (`8540167`/`8540230`) as the only remaining clean-training
    candidate from the original factorial. First attempt died at ~9min on
    the blendcorpus index race (768 ranks racing rank-0's
    `_build_index_mappings` on a fresh CKPT_DIR; "mmap length is greater
    than file size"). Resubmitted (`8540230`) after pre-warming the
    data-cache via symlink from the validated n=32 TP=4 fp32-mp run's
    cache (content-keyed, so any prior 80B-on-olmo-mix-1124 cache
    works). Resubmit running.
- **80B scaling sweep**: 5 one-off smoke scripts at N=16/32/64/128/256
  ([`6f38e81e1`](https://github.com/saforem2/torchtitan/commit/6f38e81e1)) +
  n=32 LR=1e-7 retry script
  ([`ba221735a`](https://github.com/saforem2/torchtitan/commit/ba221735a)) +
  n=64/128/256 deterministic sweep scripts with overprovisioning
  ([`35c3d966d`](https://github.com/saforem2/torchtitan/commit/35c3d966d))
  and n=64/n=128 fp32-mp scripts
  ([`3f42b0135`](https://github.com/saforem2/torchtitan/commit/3f42b0135),
  [`e457293e5`](https://github.com/saforem2/torchtitan/commit/e457293e5)).

## 2. SFT recipe (2B × tulu_math_uc_mix)

- Reached `Training complete.` after 729 steps × 3 epochs × GBS=6144 (4 PBS
  jobs, 8 autoretry attempts surviving 3 SIGABRTs). Final checkpoint:
  `outputs/sft/aurora2b-sophiag-tulu-mix-32n-gbs6144/checkpoint-729-hf/`.
- Recipe writeup: [`production/sft/aurora2b/tulu_math_uc_mix/`](../production/sft/aurora2b/tulu_math_uc_mix/README.md)
  (promoted to recipe tree in
  [`9c986cbb2`](https://github.com/saforem2/torchtitan/commit/9c986cbb2)).
- SFT data infrastructure: tulu-3 / OpenMathInstruct-2 / ultrachat mix loader
  ([`890db2925`](https://github.com/saforem2/torchtitan/commit/890db2925)),
  content-hashed disk cache for interleaved mixes
  ([`0f6692388`](https://github.com/saforem2/torchtitan/commit/0f6692388)),
  `--max_train_samples` flag
  ([`2997f4f4b`](https://github.com/saforem2/torchtitan/commit/2997f4f4b)),
  `--resume_from_checkpoint` autoretry-survival
  ([`65de1af71`](https://github.com/saforem2/torchtitan/commit/65de1af71),
  [`4c9d1ede8`](https://github.com/saforem2/torchtitan/commit/4c9d1ede8)),
  ShardedTensor.device patch for XPU FSDP resume
  ([`160afdac9`](https://github.com/saforem2/torchtitan/commit/160afdac9);
  upstream fix tracked via
  [`docs/upstream-issues/`](../upstream-issues/) and pytorch/pytorch#186938,
  #186940).
- **IFEval ran end-to-end** — SFT'd model wins on instruction-following vs base
  ([`c4a3c5c01`](https://github.com/saforem2/torchtitan/commit/c4a3c5c01)).
  Writeup: [`production/sft/aurora2b/tulu_math_uc_mix/evals/`](../production/sft/aurora2b/tulu_math_uc_mix/evals/).
- 6-panel SFT training-curves chart (loss / grad_norm / LR / token accuracy /
  entropy / tokens-seen) with PBS-attempt shading
  ([`1ec91847d`](https://github.com/saforem2/torchtitan/commit/1ec91847d),
  restyled in ambivalent + Iosevka via
  [`45b1a17d0`](https://github.com/saforem2/torchtitan/commit/45b1a17d0)).

## 3. GRPO recipe (2B SFT'd × sum_digits arithmetic, 8N)

- **Smoke first**: 4N smoke confirmed SFT-step729 vs baseline as RL
  initialization. SFT'd model converges ~8× faster on `accuracy_reward` (0.92
  vs 0.12 mean of last 10 steps), solves task by step 40 (baseline never
  crosses 30% in 50 steps). Writeup:
  [`production/sft/aurora2b/tulu_math_uc_mix/evals/grpo-smoke.md`](../production/sft/aurora2b/tulu_math_uc_mix/evals/grpo-smoke.md).
- **Production run completed**: 1000 GRPO steps at 8N, lr=1e-6, bf16. Final
  checkpoint at `outputs/grpo/aurora2b-sft-arithmetic-8n/checkpoint-1000/`
  (W&B: [`7ktk55uz`](https://wandb.ai/aurora_gpt/torchtitan.ezpz.rl/runs/7ktk55uz)).
  Submit script:
  [`rl/scripts/grpo/aurora2b_sft_arithmetic_8n.sh`](../../rl/scripts/grpo/aurora2b_sft_arithmetic_8n.sh)
  ([`22394ea85`](https://github.com/saforem2/torchtitan/commit/22394ea85)).
- Trajectory + 9-panel chart:
  [`production/grpo/aurora2b/sft_arithmetic/`](../production/grpo/aurora2b/sft_arithmetic/README.md)
  ([`1ca0c946f`](https://github.com/saforem2/torchtitan/commit/1ca0c946f)).

## 4. vLLM-XPU investigation (RL inference path)

- vLLM-XPU end-to-end working on torch 2.13 via **Option B sibling venv** — a
  self-contained `vllm-test` venv that doesn't disturb the main `.venv`.
  Findings: [`0600b8b4a`](https://github.com/saforem2/torchtitan/commit/0600b8b4a),
  end-to-end validation:
  [`f72de700e`](https://github.com/saforem2/torchtitan/commit/f72de700e).

## 5. Evals

- Backfill scripts re-eval all v2 ckpts through the new 7-task set
  (added piqa, openbookqa, boolq —
  [`8451b53fb`](https://github.com/saforem2/torchtitan/commit/8451b53fb),
  [`221754a79`](https://github.com/saforem2/torchtitan/commit/221754a79)).
- 20B step-100..800 eval chain backfilled to 36/36 7-task data points
  ([`75dd3fe30`](https://github.com/saforem2/torchtitan/commit/75dd3fe30)).
  Refresh: [`docs/evals/agpt/20b/`](../evals/agpt/20b/README.md).
- Renamed `plot_v1_vs_v2.py` → `plot_eval_overview.py` + added
  `figures/eval_overview.svg`
  ([`a00865541`](https://github.com/saforem2/torchtitan/commit/a00865541)).
- Eval backfill oneoff scripts now delegate to canonical
  `eval-{2b,20b}-v2.sh` instead of duplicating logic
  ([`61dc819ea`](https://github.com/saforem2/torchtitan/commit/61dc819ea)).

## 6. Upstream syncs

Six syncs absorbed in the past week. Full log at
[`docs/upstream-sync.md`](../upstream-sync.md).

| Sync | Date | Commits | Replays | Outcome |
|---|---|--:|---|---|
| 50th | 2026-06-09 | 2 | `global_valid_tokens` retype | IDENTICAL (job 12468341) |
| 51st | 2026-06-09 | 9 | PR #3571 drop GQAttention mask_type | IDENTICAL (job 12468399) |
| 52nd | 2026-06-12 | 18 | PR #3643 spmd.R→I + PR #3626 expert FQN rename + PR #3641 `get_train_context` keyword-only | IDENTICAL agpt (job 12468664) + ≤4e-4 nats MoE (job 12468666) |
| 53rd | 2026-06-12 | 2 | none (CheckpointManager/ModelWrapper exports preserved, topk revert auto-flows) | Static imports OK |
| 54th | 2026-06-12 | 3 | none (MoE SP combine fix flows automatically; our sp_size=1 so bug never live; rl/vllm + ROCm CI don't touch ezpz) | IDENTICAL agpt (job 12468696) + clean 10-step MoE smoke (job 12468697) |
| 55th | 2026-06-12 | 3 | none (all three touch upstream-only code paths ezpz consumes via import) | Static imports OK; smoke deferred (last sync's `ChunkedCELoss` bitwise IDENTICAL covers the path) |

Plus a separate replay for upstream PR #3586 (`global_valid_tokens: float|None`).

## 7. PR #14 review (saforem2/torchtitan#14, "Isolate ezpz MoE" by Nathan)

Multi-day code review on a 2,878-line MoE-isolation PR from a contributor:

- Ran `/code-review --effort high` → 11 findings, 9 confirmed real (3 🔴
  correctness, 5 🟡 cleanups, 1 dead-code removal). 2 retracted as false
  positives with public correction
  ([comment](https://github.com/saforem2/torchtitan/pull/14#issuecomment-4681070694)).
- Implemented all 9 fixes on a local `pr14-fixes` branch (12 commits including a
  separately-discovered #3571 sync-drift fix).
- **A/B numerics verification** across 6 configurations: debug-model
  bit-identical, EP=1/2/4/6/12 with and without padding fast-path. Every
  multi-node config agrees to within FP-summation noise (≤2e-2 nats / 0.4
  grad_norm). 1N EP=12 outlier identified as XCCL intra-node artifact, not
  PR-related. Posted as
  [PR comment](https://github.com/saforem2/torchtitan/pull/14#issuecomment-4684931732).
- **`olmo-mix-1124` 8N investigation**: confirmed the
  `_rank0_prefetch_then_barrier` wrapper works (96/96 ranks succeed in 12s)
  but the data CDN saturates with 96 concurrent shard opens. Pre-download +
  `register_local_dataset` is the durable answer. Added
  [`precache_hf_dataset.py`](../../scripts/precache_hf_dataset.py)
  helper + [docs guide](../guides/hf-dataset-offline-cache.md) +
  `olmo_mix_wiki_local` registration validated at 8N (5 steps clean,
  MFU 20%).
- Nathan re-ran his microbenchmark on pr14-fixes: monotonic positive speedup
  curve **1.00× → 1.94×** across EP=1→12 (was mixed positive/negative on his
  original branch —
  [his table](https://github.com/saforem2/torchtitan/pull/14#issuecomment-4683044528)).
- 6 long-form comments on the PR thread; pr14-fixes branch ready to merge.

## 8. HF-dataset infrastructure

- `_rank0_prefetch_then_barrier` in
  [`ezpz/datasets.py`](../../datasets.py) for HF rate-limit avoidance
  ([`1ea2dce76`](https://github.com/saforem2/torchtitan/commit/1ea2dce76)).
- `register_local_dataset` extended with `format=` param (parquet/json/arrow/csv/text)
  ([`05e743186`](https://github.com/saforem2/torchtitan/commit/05e743186)) —
  fixes `.json.gz` olmo-mix loading.
- [`precache_hf_dataset.py`](../../scripts/precache_hf_dataset.py)
  ([`aa040e303`](https://github.com/saforem2/torchtitan/commit/aa040e303)) +
  [docs guide](../guides/hf-dataset-offline-cache.md)
  ([`e82cf6d5b`](https://github.com/saforem2/torchtitan/commit/e82cf6d5b))
  with the validated 8N recipe.

## 9. Charts + visualization standardization

- Shared [`utils/plot_style.py`](../../utils/plot_style.py) helper applies
  `ambivalent` theme + Iosevka font in one `apply_style()` call
  ([`7cec4edcc`](https://github.com/saforem2/torchtitan/commit/7cec4edcc)).
- Iosevka downloaded once to `~/.local/share/fonts/Iosevka/`.
- Wired into 4 chart scripts:
  [`plot_production_wandb.py`](../../utils/plot_production_wandb.py),
  [`plot_production_combined.py`](../../utils/plot_production_combined.py),
  [SFT curves](../production/sft/aurora2b/tulu_math_uc_mix/scripts/plot_sft_curves.py),
  [GRPO curves](../production/grpo/aurora2b/sft_arithmetic/scripts/plot_grpo_curves.py).
- All 2B + 20B production figures (svg + png) regenerated in the new style
  ([`1b43fb152`](https://github.com/saforem2/torchtitan/commit/1b43fb152)).
- **Per-model production overlay split (2026-06-12,
  [`c26e33de1`](https://github.com/saforem2/torchtitan/commit/c26e33de1))**:
  `plot_production_combined.py` now emits per-model SVG/PNG
  (`production_2b_training`, `production_20b_training`) alongside the
  combined chart. The 2B/20B per-model READMEs embed only their own
  model's chart + link to the cross-model overlay for context — earlier
  decision to embed the all-models chart on every page was incorrect.
  80B sub-README drops the chart entirely (no live 80B W&B data yet).
- **Top-level `docs/production/README.md` refresh (2026-06-12,
  [`c26e33de1`](https://github.com/saforem2/torchtitan/commit/c26e33de1)
  + [`434cfe5d1`](https://github.com/saforem2/torchtitan/commit/434cfe5d1))**:
  bumped from "Last updated 2026-06-08" with stale 2B/20B/80B canonical
  chain numbers to current state. The stale-doc checker only covers
  per-chain READMEs and missed this top-level one.

## 10. Operational fixes

- **`os._exit(0)` end-of-main** in [`ezpz/train.py`](../../train.py)
  ([`7d2aa6e20`](https://github.com/saforem2/torchtitan/commit/7d2aa6e20)):
  terminates the chronic post-`Process group destroyed` hang where mpiexec
  waits on wedged C++ signal-handler threads on most ranks. Root-caused via
  `/proc/<pid>/wchan` inspection (one thread in `__do_sys_pause`, non-daemon
  → Python can't shut down).
- **SIGKILL `resource_tracker` daemon** before `os._exit`
  ([`31ad6049a`](https://github.com/saforem2/torchtitan/commit/31ad6049a))
  to suppress the per-semaphore "leaked semaphore" cosmetic warnings the
  daemon emits on parent disappearance.
- **Log noise cleanup** ([`d0be65fc1`](https://github.com/saforem2/torchtitan/commit/d0be65fc1)):
  collapsed the per-layer `grouped_mm requires SM90+` warning (26 → 1 on
  rank 0 via `warn_once`); gagged the Python `warnings` module on non-zero
  ranks via `warnings.filterwarnings("ignore")` in `init_logger` — kills
  inductor + autocast warning fan-out without losing rank-0 visibility.
- **`bitwise_sync_check.sh`** gained `HEAD_COMMIT` env override for
  nested-worktree runs
  ([`ea169b7a9`](https://github.com/saforem2/torchtitan/commit/ea169b7a9)).

## 11. Counts

- 71 PBS jobs in the `12468XXX` series (production training + smokes + bitwise checks + olmo-mix probes).
- ~140 commits authored by me on `ezpz` (plus ~30 upstream merges from pytorch/torchtitan).
- 4 active worktrees: `pr14-fixes` (review work), `ezpz-52nd-sync` (sync), `ezpz-moe-integration`, `ezpz-46th-47th-sync`.
- 6 long-form PR #14 comments.

## What's open

- Push `pr14-fixes` branch back to Nathan's PR fork (task #38 — blocked on auto-mode classifier; commits are pushed to your fork, comment-trail is complete).
- 80B production GRPO chain (task #21) — submitted, autoretry in flight.
- **80B NaN — fp32-mp scaling probe in flight (`8540230` n=64 TP=4 LBS=1
  GBS=192, resubmit after blendcorpus race ate the first attempt).** If
  clean, follow up with n=128 TP=4 LBS=1 GBS=384 (script ready as
  `n128-tp4-fp32-mp.sh`, not yet submitted). If clean at GBS=384, that's
  the production-restart config (~75% throughput cost vs bf16-acts).
  If n=64 or n=128 NaN, fp32-mp also doesn't scale and we're out of
  factorial candidates — next move would be LR=1e-8 probe, gradient
  accumulation tricks, or escalating to ALCF for the Aurora bf16-matmul
  behavior at 80B scale.
